bool comp (pair<int,int>p1 , pair<int, int> p2){
     if(p1,second < p2.second) return true;
     if(p1,second > p2.second) return false;

     //they are same
     if(p1.first > p2.first) return true;
     return false;
      
}

void SomeAlogos(){

    sort(a,a+n);
    sprt(v.begin(),v.end());

    sort(a+2,a+4);

    sort((a,a+n. greater<int>));  //descedening order

    pair<int,int> a[]={ {1,2}, {2,1}, {4,1} };

    //sort is according to the 2nd element ie {x,y} acc to y
    // and if 2nd element is same then solrt it according to the
    //1st element but im=n decreasing order

    // then we use
    sort(a, a+n, comp);  //comp is written above ie self 
    //written comparitor n boolean func

    int nu =7;
    iny cnt=  _builtin_popcount();

    long long num= 34253252346;
    int cnt= _builtin_countll();

    string s="123";  // if u wat all the permutations

    do{
        count<<s<<enld;
    } while(next_permutation(s.begin(),s.end()));

    int maxi= *max_element(a,a+n);

}

int main(){

    return 0;
}