#include<bits/stdc++.h>
using namespace std;

void pattern(int n){
     
}

int main(){
    int t;
    cout << "Enter the number of test cases: ";
    cin >> t;
    
    while(t--){
        int n;
        cout << "Enter the number of rows: ";
        cin >> n;
        
        pattern(n);
        cout << endl;
    }
    
    return 0;
}